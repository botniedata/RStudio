# Introduction to R ----
## str() function ----

# str() function in R is a powerful tool for examining the structure of an object. It provides a concise summary of the object's type, dimensions, and the first few elements.

# For restarting environment
rm(list = ls())

# Investigate the structure of mtcars
str(mtcars)