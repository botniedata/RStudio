# Introduction to R ----
## Data Type Class Function ----

# class() is a built-in function in R that is used to determine the data type of an object. It returns a character string specifying the class of the object.

# For restarting environment
rm(list = ls())

# Declared variable of different types
my_number <- 10
my_character <- "Hello World"
my_logical <- FALSE

# Check class of my_number
class(my_number)

# Check class of my_character
class(my_character)

# Check class of my_logical
class(my_logical)